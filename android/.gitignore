*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# Firebase client config — registered per-environment in the Firebase
# console, never committed.
app/google-services.json
